const superHeroes: string[] = [];

const heroPower: Array<number> = [];
// const heroPower: number[] = [];
// both are same

superHeroes.push("thor", "spiderman", "ironman");
heroPower.push(2);

type User = {
  name: string;
  isActive: boolean;
};

const allUser: User[] = [];
allUser.push({ name: "rj", isActive: true });

const MLModels: number[][] = [
  [255, 255, 255],
  [10, 13],
];

export {};
